server:
  port: ${SERVER_PORT}

spring:
  datasource:
    url: jdbc:mysql://${host_name}:${db_port}/${db_name}?createDatabaseIfNotExist=true
    username: ${db_username}
    password: ${db_password}
    jpa:
      hibernate:
        ddl-auto: update
